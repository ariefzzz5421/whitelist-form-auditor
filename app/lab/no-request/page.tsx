import { LabForm } from "../LabForm";
export default function Page() { return <LabForm endpoint={null} title="No request" description="Submit is handled entirely in the browser, so the auditor should classify this as NO_SUBMISSION_DETECTED." />; }
